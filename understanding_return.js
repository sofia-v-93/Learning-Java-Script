"use strict";

let run = function(x, y) {
  x = x * 5;
  return x + y
  alert("test")
  /*return beendet die Ausführung einer Funktion der alert wird daher nicht mehr angezeigt*/
}
let result = run(1, 2);
console.log(result);
